services:
  autotests:
    build:
      context: .
    container_name: ketcher-autotest
    env_file:
      .env
    network_mode: "host"
    extra_hosts:
      - host:docker:internal:4002
      - host.docker.internal:host-gateway
      - host.docker.internal:4002:host-gateway
    volumes:
      - ./:/app
      - /app/node_modules
